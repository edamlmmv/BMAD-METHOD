# OpenClaw runtime secrets and environment.
# Fill these in before expecting live runtime checks to pass.
# Populate the key that matches the provider/model path you actually choose.
# OpenRouter is optional; leave unused providers blank.

OPENROUTER_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENCLAW_GATEWAY_TOKEN={{generated_gateway_token}}
