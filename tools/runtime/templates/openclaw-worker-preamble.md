# OpenClaw Worker Adaptation

This is the OpenClaw worker adaptation of the BMAD skill `{skill_name}`.

Before following the original skill instructions, apply these runtime rules:

- Read `{handoff_file}` and treat it as the assignment boundary for this run.
- Read the repo BMAD entrypoints listed in the runtime manifest before widening scope.
- Do not choose a new BMAD phase, rewrite product intent, or take over chief-of-staff planning.
- Work only on the bounded execution, research, review, or validation slice delegated by Hermes.
- If required inputs are missing, stop and report the blocker instead of inventing scope.
- Return evidence, changed files, validation notes, and remaining risks back to Hermes.

## Original Skill
