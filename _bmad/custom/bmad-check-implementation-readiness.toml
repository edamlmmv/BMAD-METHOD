[workflow]
persistent_facts = [
  "For Office.js leverage planning, readiness must evaluate product brief, source register, graph preflight, glossary, capability matrix, ADRs, backlog, TDD plan, and traceability matrix together.",
  "Implementation readiness is blocked if graph evidence is missing/invalid, source refs are uncited, ADRs lack rollback/platform notes, or stories lack first failing public behavior tests.",
  "Known unrelated source dirt: /Users/edam/Documents/TODA/MissionCTL/office-js/.DS_Store. Never stage it.",
  "Before any future push from BMAD-METHOD, run npm ci && npm run quality on the exact HEAD being pushed.",
  "When readiness depends on Playwright CLI, Agent Browser CLI, Browser Use IAB, or Computer Use MCP, require manual evidence with tool, timestamp, environment, action summary, artifact refs, pass/fail, and explicit verifier/grant/runtime boundary.",
  "Browser and desktop affordance readiness never changes Workspace Capability Request schema, Workspace CLI behavior, or verifier authority.",
  "When readiness depends on Git, require HEAD and base refs, dirty state, staged/unstaged/untracked status, patch evidence, changed files, rollback note, Conventional Commit plan, and exact npm ci && npm run quality gate evidence before push.",
  "Git readiness is blocked when Git evidence is missing, ambiguous, destructive, stale, or not tied to Workspace result, review, closeout, or archive evidence.",
  "Git CLI status and diff output are manual Worktree Review evidence only; they are not Capability Verifier input, Grant Guard authority, runtime authority, push permission, reset permission, clean permission, merge permission, or hidden execution.",
  "When readiness depends on Context7 Docs MCP, require manual evidence that cites docs source URLs, records Apple Passwords item Context7 as credential source, records only CONTEXT7_API_KEY=set or unset, and never stores the API key value.",
  "Context7 readiness is blocked if docs evidence implies Git operations, live MCP authority, verifier authority, keychain automation, or raw secret storage.",
  "When readiness depends on Git MCP, require manual/grant-gated evidence for add, commit, and branch tools, keep GitHub connector state separate, and preserve local git CLI plus npm ci && npm run quality as exact pre-push authority.",
  "When readiness depends on Codex MCP/TOML docs readiness, cite https://developers.openai.com/codex/config-reference#configtoml and https://developers.openai.com/codex/cli/reference#codex-mcp, separate mcp_servers.*, codex mcp, ~/.codex/config.toml, .codex/config.toml, and OpenAI Docs MCP as docs/operator context only, and require self-contained Capability Request JSON for verifier compatibility.",
  "Codex MCP/TOML docs readiness is blocked when local Codex config, OpenAI Docs MCP output, mcp_servers.*, codex mcp, ~/.codex/config.toml, or .codex/config.toml are treated as verifier proof; record validation commands separately with not verifier input language and no Workspace verifier, CLI, schema, or capability registry changes.",
  "When readiness depends on PostgreSQL MCP, require fixture-backed declared capability evidence for host.mcp.postgresql.readonly, operator-owned POSTGRES_URL=set or unset only, least-privilege read-only scope, allowed schemas/tables, denied writes, and explicit not-verifier-input boundary.",
  "PostgreSQL MCP readiness is blocked if read-only database evidence need is unexplained, connection string values are stored, schemas/tables are unbounded, denied writes are missing, or live PostgreSQL/Docker/MCP state is treated as Workspace verifier authority.",
  "Tool leverage review: Before implementation, consider known BMAD skills, repo scripts, MCP tools, browser affordances, or automation that could improve evidence, speed, repeatability, or safety. Record the decision and rationale: use existing tool, proceed manually with reason, propose tooling enhancement later, or enhance tooling before implementation."
]
